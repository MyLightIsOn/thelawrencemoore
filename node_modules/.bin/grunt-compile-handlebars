#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-compile-handlebars/bin/grunt-compile-handlebars" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-compile-handlebars/bin/grunt-compile-handlebars" "$@"
  ret=$?
fi
exit $ret
